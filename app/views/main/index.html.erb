		<div id="pxs_container" class="pxs_container">
			<div class="pxs_bg">
				<div class="pxs_bg1"></div>
			</div>
			<div class="pxs_slider_wrapper">
				<ul class="pxs_slider">
					<li><div style="background:url('images/5.jpg'); margin:0 auto; width:1000px; height:100%;">aaa</div></li>
					<li><img src="images/6.jpg" alt="Second Image" /></li>
					<li><img src="images/5.jpg" alt="Third Image" /></li>
					<li><img src="images/6.jpg" alt="Forth Image" /></li>
					<li><img src="images/5.jpg" alt="Fifth Image" /></li>
					<li><img src="images/6.jpg" alt="Sixth Image" /></li>
					<li><img src="images/6.jpg" alt="Sixth Image" /></li>
					<li><img src="images/6.jpg" alt="Sixth Image" /></li>
					<li><img src="images/6.jpg" alt="Sixth Image" /></li>
				</ul>
				<ul class="pxs_thumbnails">          
					<li><img src="images/thumbs/5.jpg" alt="First Image" class="selected"/><span>하이</span></li>
					<li><img src="images/thumbs/6.jpg" alt="Second Image"class="selected"/></li>
					<li><img src="images/thumbs/5.jpg" alt="Third Image" class="selected"/></li>
					<li><img src="images/thumbs/6.jpg" alt="Forth Image" class="selected"/></li>
					<li><img src="images/thumbs/5.jpg" alt="Fifth Image" /></li>
					<li><img src="images/thumbs/6.jpg" alt="Sixth Image" /></li>
					<li><img src="images/thumbs/6.jpg" alt="Sixth Image" /></li>
					<li><img src="images/thumbs/6.jpg" alt="Sixth Image" /></li>
					<li><img src="images/thumbs/6.jpg" alt="Sixth Image" /></li>
				</ul>
				<div class="pxs_navigation2">
					<span class="pxs_prev"></span>
					<span class="pxs_next"></span>
				</div>
				<div class="pxs_navigation">
					<span class="pxs_prev"></span>
					<span class="pxs_next"></span>
				</div>
			</div>
      <img id="currentImage" style="position:absolute; z-index:-1; left:622px;top:525px;"src="/images/main.png"/>
		</div>

		<!-- The JavaScript -->
        <script type="text/javascript">
			(function($) {
				$.fn.parallaxSlider = function(options) {
					var opts = $.extend({}, $.fn.parallaxSlider.defaults, options);
					return this.each(function() {
						var $pxs_container 	= $(this),
						o 				= $.meta ? $.extend({}, opts, $pxs_container.data()) : opts;
						
						//the main slider
						var $pxs_slider		= $('.pxs_slider',$pxs_container),
						//the elements in the slider
						$elems			= $pxs_slider.children(),
						//total number of elements
						total_elems		= $elems.length,
						//the navigation buttons
						$pxs_next		= $('.pxs_next',$pxs_container),
						$pxs_prev		= $('.pxs_prev',$pxs_container),
						//the bg images
						$pxs_bg1		= $('.pxs_bg1',$pxs_container),
						$pxs_bg2		= $('.pxs_bg2',$pxs_container),
						$pxs_bg3		= $('.pxs_bg3',$pxs_container),
						//current image
						current			= 3,
						//the thumbs container
						$pxs_thumbnails = $('.pxs_thumbnails',$pxs_container),
						//the thumbs
						$thumbs			= $pxs_thumbnails.children(),
						//the interval for the autoplay mode
						slideshow,
						//the loading image
						$pxs_slider_wrapper = $('.pxs_slider_wrapper',$pxs_container);
							
						//first preload all the images
						var loaded		= 0,
						$images		= $pxs_slider_wrapper.find('img');
							
						$images.each(function(){
							var $img	= $(this);
							$('<img/>').load(function(){
								++loaded;
								if(true){
									$pxs_slider_wrapper.show();
										
									//one images width (assuming all images have the same sizes)
									var one_image_w		= '840';
							
									/*
									need to set width of the slider,
									of each one of its elements, and of the
									navigation buttons
									 */
									setWidths($pxs_slider,
									$elems,
									total_elems,
									$pxs_bg1,
									$pxs_bg2,
									$pxs_bg3,
									one_image_w,
									$pxs_next,
									$pxs_prev);
							
									/*
										set the width of the thumbs
										and spread them evenly
									 */
									$pxs_thumbnails.css({
										'width'			: 840 + 'px',
										'margin-left' 	: -495 + 'px'
									});
									var spaces	= one_image_w/(7);
									$thumbs.each(function(i){
										var $this 	= $(this);
										var left	= spaces*(i+1) - $this.width()/2;
										$this.css('left',left+'px');
											
										//hovering the thumbs animates them up and down
										$this.bind('mouseenter',function(){
											$(this).stop().animate({top:'-10px'},100);
										}).bind('mouseleave',function(){
											$(this).stop().animate({top:'0px'},100);
										});
									});
										
									//make the first thumb be selected
									highlight($thumbs.eq(3));
										
										slide(3,
										$pxs_slider,
										$pxs_bg3,
										$pxs_bg2,
										$pxs_bg1,
										o.speed,
										o.easing,
										o.easingBg);
                    $($thumbs.eq(0)).addClass('selected');

                    $($thumbs.eq(1)).addClass('selected');

                    $($thumbs.eq(2)).addClass('selected');

                    $($thumbs.eq(3)).addClass('selected');

                    $($thumbs.eq(4)).addClass('selected');

                    $($thumbs.eq(5)).addClass('selected');

                    $($thumbs.eq(6)).addClass('selected');
									//slide when clicking the navigation buttons
									$pxs_next.bind('click',function(){
										++current;
										if(current >= total_elems)
											if(o.circular)
												current = 0;
										else{
											--current;
											return false;
										}

										highlight($thumbs.eq(current));
										slide(current,
										$pxs_slider,
										$pxs_bg3,
										$pxs_bg2,
										$pxs_bg1,
										o.speed,
										o.easing,
										o.easingBg);

                    $($thumbs.eq(current + 3)).addClass('selected');
                    $($thumbs.eq(current - 3)).addClass('selected');

                    $($thumbs.eq(current - 2)).addClass('selected');

                    $($thumbs.eq(current - 1)).addClass('selected');

                    $($thumbs.eq(current)).addClass('selected');

                    $($thumbs.eq(current + 1)).addClass('selected');

                    $($thumbs.eq(current + 2)).addClass('selected');
                    if (current < total_elems - 4){
                      $($thumbs.eq(current - 4)).removeClass('selected');
                    }
                    else{
                      $($thumbs.eq(total_elems - 7)).addClass('selected');
                      $($thumbs.eq(total_elems - 6)).addClass('selected');
                      $($thumbs.eq(total_elems - 5)).addClass('selected');

                    }

                    if (current > 2){
                      $($thumbs.eq(current + 4)).removeClass('selected');
                    }
                    else{
                      $($thumbs.eq(4)).addClass('selected');
                      $($thumbs.eq(5)).addClass('selected');
                      $($thumbs.eq(6)).addClass('selected');
                      $($thumbs.eq(7)).removeClass('selected');
                      $($thumbs.eq(8)).removeClass('selected');
                      $($thumbs.eq(9)).removeClass('selected');

                    }

									});

									$pxs_prev.bind('click',function(){
										--current;
										if(current < 0)
											if(o.circular)
												current = total_elems - 1;
										else{
											++current;
											return false;
										}
										highlight($thumbs.eq(current));
										slide(current,
										$pxs_slider,
										$pxs_bg3,
										$pxs_bg2,
										$pxs_bg1,
										o.speed,
										o.easing,
										o.easingBg);
                    $($thumbs.eq(current - 3)).addClass('selected');
                    $($thumbs.eq(current + 3)).addClass('selected');
                    $($thumbs.eq(current - 2)).addClass('selected');
                    $($thumbs.eq(current - 1)).addClass('selected');
                    $($thumbs.eq(current)).addClass('selected');

                    $($thumbs.eq(current + 1)).addClass('selected');

                    $($thumbs.eq(current + 2)).addClass('selected');
                    if (current < total_elems - 4){
                      $($thumbs.eq(current - 4)).removeClass('selected');
                    }
                    else{
                      $($thumbs.eq(total_elems - 7)).addClass('selected');
                      $($thumbs.eq(total_elems - 6)).addClass('selected');
                      $($thumbs.eq(total_elems - 5)).addClass('selected');
                    }
                    if (current > 2){
                      $($thumbs.eq(current + 4)).removeClass('selected');
                    }
                    else{
                      $($thumbs.eq(4)).addClass('selected');
                      $($thumbs.eq(5)).addClass('selected');
                      $($thumbs.eq(6)).addClass('selected');
                      $($thumbs.eq(7)).removeClass('selected');
                      $($thumbs.eq(8)).removeClass('selected');
                      $($thumbs.eq(9)).removeClass('selected');
                      

                    }


									});
							
									/*
									clicking a thumb will slide to the respective image
									 */
									$thumbs.bind('click',function(){
										var $thumb	= $(this);
										highlight($thumb);
										//if autoplay interrupt when user clicks
										if(o.auto)
											clearInterval(slideshow);
										current 	= $thumb.index();
										slide(current,
										$pxs_slider,
										$pxs_bg3,
										$pxs_bg2,
										$pxs_bg1,
										o.speed,
										o.easing,
										o.easingBg);
                    $($thumbs.eq(current - 4)).removeClass('selected');
                    $($thumbs.eq(current + 4)).removeClass('selected');
                    $($thumbs.eq(current - 3)).addClass('selected');

                    $($thumbs.eq(current + 3)).addClass('selected');

                    $($thumbs.eq(current - 2)).addClass('selected');

                    $($thumbs.eq(current - 1)).addClass('selected');

                    $($thumbs.eq(current)).addClass('selected');

                    $($thumbs.eq(current + 1)).addClass('selected');

                    $($thumbs.eq(current + 2)).addClass('selected');
									});
							
								
							
									/*
									activate the autoplay mode if
									that option was specified
									 */
									if(o.auto != 0){
										o.circular	= true;
										slideshow	= setInterval(function(){
											$pxs_next.trigger('click');
										},o.auto);
									}
							
									/*
									when resizing the window,
									we need to recalculate the widths of the
									slider elements, based on the new windows width.
									we need to slide again to the current one,
									since the left of the slider is no longer correct
									 */
									$(window).resize(function(){
										w_w	= $(window).width();
										setWidths($pxs_slider,$elems,total_elems,$pxs_bg1,$pxs_bg2,$pxs_bg3,one_image_w,$pxs_next,$pxs_prev);
										slide(current,
										$pxs_slider,
										$pxs_bg3,
										$pxs_bg2,
										$pxs_bg1,
										1,
										o.easing,
										o.easingBg);
									});

								}
							}).error(function(){
								alert('here')
							}).attr('src',$img.attr('src'));
						});
							
							
							
					});
				};
				
				//the current windows width
				var w_w				= $(window).width();
				
				var slide			= function(current,
				$pxs_slider,
				$pxs_bg3,
				$pxs_bg2,
				$pxs_bg1,
				speed,
				easing,
				easingBg){
					var slide_to	= parseInt(-w_w * current);
          var total_elems = $pxs_slider.children().length;
					$pxs_slider.stop().animate({
						left	: slide_to + 'px'
					},speed, easing);


          if (current > 2 && current < total_elems - 3 ){
            $('.pxs_thumbnails').stop().animate({
                left: (1060 - 120*(current)) + 'px'
            },500);    
            $('#currentImage').stop().animate({
                left: '622px' },500);
            }
          else{
            $('.pxs_thumbnails').stop().animate({
                left: (1060 - 120*(3)) + 'px'
            },500);    
            if (current == 2){
              $('#currentImage').stop().animate({
              left: '502px' },500);
            }
            else if (current == 1){
              $('#currentImage').stop().animate({
              left: '382px'},500);
            }
            else if (current == 0){ 
              $('#currentImage').stop().animate({
              left: '262px'},500);
            }
          }

          if (current > total_elems - 4){
            $('.pxs_thumbnails').stop().animate({
                left: (1060 - 120*(total_elems - 4)) + 'px'
            },500); 

            if (current == total_elems - 3){
              $('#currentImage').stop().animate({
              left: '742px' },500);
            }
            else if (current == total_elems - 2){
              $('#currentImage').stop().animate({
              left: '862px'},500);
            }
            else if (current == total_elems - 1){ 
              $('#currentImage').stop().animate({
              left: '982px'},500);
            }
          }

				}
				
				var highlight		= function($elem){
					$elem.siblings().removeClass('selected');
					$elem.addClass('selected');
				}
				
				var setWidths		= function($pxs_slider,
				$elems,
				total_elems,
				$pxs_bg1,
				$pxs_bg2,
				$pxs_bg3,
				one_image_w,
				$pxs_next,
				$pxs_prev){
					/*
					the width of the slider is the windows width
					times the total number of elements in the slider
					 */
					var pxs_slider_w	= w_w * total_elems;
					$pxs_slider.width(pxs_slider_w + 'px');
					//each element will have a width = windows width
					$elems.width(w_w + 'px');
					/*
					we also set the width of each bg image div.
					The value is the same calculated for the pxs_slider
					 */
					$pxs_bg1.width(pxs_slider_w + 'px');
					$pxs_bg2.width(pxs_slider_w + 'px');
					$pxs_bg3.width(pxs_slider_w + 'px');
					
					/*
					both the right and left of the
					navigation next and previous buttons will be:
					windowWidth/2 - imgWidth/2 + some margin (not to touch the image borders)
					 */
				}
				
				$.fn.parallaxSlider.defaults = {
					auto			: 0,	//how many seconds to periodically slide the content.
											//If set to 0 then autoplay is turned off.
					speed			: 500,//speed of each slide animation
					easing			: 'jswing',//easing effect for the slide animation
					easingBg		: 'jswing',//easing effect for the background animation
					circular		: true,//circular slider
					thumbRotation	: true//the thumbs will be randomly rotated
				};
				//easeInOutExpo,easeInBack
			})(jQuery);
		</script>

		<script type="text/javascript">
			$(function() {
				var $pxs_container	= $('#pxs_container');
				$pxs_container.parallaxSlider();
			});
      </script>

